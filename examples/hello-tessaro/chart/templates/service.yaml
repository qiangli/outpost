apiVersion: v1
kind: Service
metadata:
  name: {{ include "hello-tessaro.fullname" . }}
  labels: {{- include "hello-tessaro.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector: {{- include "hello-tessaro.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
